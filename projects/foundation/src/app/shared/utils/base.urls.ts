/**
 * The base URLS for API.
 * @constant
 */
export const BASE_URLS = {
    ACCOUNTS_TYPE: {
        endpoint: 'accounts_type'
    },
    USERS: {
        endpoint: 'users',
        profile: 'users/profile'
    },
    AUTH: {
        endpoint: 'auth',
        token: 'auth/token',
        authenticated_user: 'auth/authenticated_user'
    },
    HEALTH_CHECK: 'health-check'
}
